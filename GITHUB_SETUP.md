# 🚀 Подключение проекта к GitHub

## ✅ Что уже сделано

- ✅ Git репозиторий инициализирован
- ✅ Создан `.gitignore` с правильными исключениями
- ✅ Сделан первый коммит со всем кодом

## 📋 Шаги для подключения к GitHub

### Вариант 1: Создать новый репозиторий на GitHub

#### 1. Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Введите название репозитория: `numerom-v2` (или любое другое)
3. Описание: `NumerOM Learning System V2 - Система обучения нумерологии`
4. Выберите **Public** или **Private**
5. **НЕ** создавайте README, .gitignore или лицензию (они уже есть)
6. Нажмите **Create repository**

#### 2. Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Выполните:

```bash
cd /Users/brandbox/Desktop/numerom-v2

# Добавьте remote (замените YOUR_USERNAME на ваш username)
git remote add origin https://github.com/YOUR_USERNAME/numerom-v2.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/numerom-v2.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

#### 3. Проверьте

Откройте https://github.com/YOUR_USERNAME/numerom-v2 - ваш код должен быть там!

---

### Вариант 2: Использовать существующий репозиторий

Если у вас уже есть репозиторий:

```bash
cd /Users/brandbox/Desktop/numerom-v2

# Добавьте remote
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Отправьте код
git push -u origin main
```

---

## 🔐 Настройка аутентификации

### Если используете HTTPS (рекомендуется для начинающих)

GitHub больше не поддерживает пароли. Используйте Personal Access Token:

1. Перейдите: https://github.com/settings/tokens
2. Нажмите **Generate new token (classic)**
3. Выберите срок действия и права доступа:
   - `repo` - полный доступ к репозиториям
4. Скопируйте токен (он больше не будет показан!)
5. При `git push` используйте токен вместо пароля

### Если используете SSH (для опытных пользователей)

1. Создайте SSH ключ (если еще нет):
```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

2. Добавьте ключ в ssh-agent:
```bash
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
```

3. Скопируйте публичный ключ:
```bash
cat ~/.ssh/id_ed25519.pub
```

4. Добавьте ключ на GitHub:
   - https://github.com/settings/keys
   - New SSH key
   - Вставьте скопированный ключ

---

## 📝 Дальнейшая работа с Git

### Базовые команды

```bash
# Проверить статус
git status

# Добавить изменения
git add .

# Сделать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push

# Получить изменения с GitHub
git pull
```

### Рабочий процесс

1. **Внесли изменения в код**
```bash
# Например, отредактировали server.py
vim backend/server.py
```

2. **Проверьте что изменилось**
```bash
git status
git diff
```

3. **Добавьте изменения**
```bash
git add backend/server.py
# Или добавьте все изменения:
git add .
```

4. **Сделайте коммит**
```bash
git commit -m "Добавлен новый API endpoint для удаления уроков"
```

5. **Отправьте на GitHub**
```bash
git push
```

---

## 🌿 Работа с ветками (опционально)

Для разработки новых функций используйте ветки:

```bash
# Создать новую ветку
git checkout -b feature/new-feature

# Работайте в ветке
git add .
git commit -m "Добавлена новая функция"

# Отправьте ветку на GitHub
git push -u origin feature/new-feature

# Переключиться обратно на main
git checkout main

# Слить ветку в main
git merge feature/new-feature

# Удалить ветку
git branch -d feature/new-feature
```

---

## 📊 Структура коммитов

Используйте понятные сообщения коммитов:

```bash
# Хорошие примеры:
git commit -m "feat: добавлен функционал удаления уроков"
git commit -m "fix: исправлена синхронизация данных в LessonEditModal"
git commit -m "docs: обновлена документация по пересборке контейнеров"
git commit -m "refactor: улучшена структура компонента AdminPanel"

# Плохие примеры:
git commit -m "изменения"
git commit -m "фикс"
git commit -m "обновление"
```

### Префиксы коммитов:
- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление зависимостей, конфигурации

---

## 🔄 Синхронизация с командой

Если работаете в команде:

```bash
# Перед началом работы получите последние изменения
git pull

# После завершения работы отправьте изменения
git push
```

---

## 🚨 Решение проблем

### Конфликты при pull

```bash
# Если возникли конфликты:
git pull
# Отредактируйте файлы с конфликтами
# Найдите маркеры: <<<<<<< HEAD, =======, >>>>>>>
# Разрешите конфликты вручную
git add .
git commit -m "Разрешены конфликты слияния"
git push
```

### Отмена изменений

```bash
# Отменить изменения в файле (до add)
git checkout -- filename

# Отменить последний коммит (сохранив изменения)
git reset --soft HEAD~1

# Отменить последний коммит (удалив изменения)
git reset --hard HEAD~1
```

### Посмотреть историю

```bash
# Список коммитов
git log

# Краткая история
git log --oneline

# История с графом веток
git log --graph --oneline --all
```

---

## 📚 Полезные ссылки

- [GitHub Docs](https://docs.github.com/)
- [Git Cheat Sheet](https://education.github.com/git-cheat-sheet-education.pdf)
- [Learn Git Branching](https://learngitbranching.js.org/)

---

## ✅ Чек-лист

- [ ] Создан репозиторий на GitHub
- [ ] Добавлен remote origin
- [ ] Выполнен первый push
- [ ] Настроена аутентификация (токен или SSH)
- [ ] Проверено что код виден на GitHub
- [ ] Добавлены коллеги (если работаете в команде)

---

**Готово!** Теперь ваш проект подключен к GitHub! 🎉

