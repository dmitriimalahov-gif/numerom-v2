# 🔄 Руководство по пересборке контейнеров

## 📋 Быстрый старт

Для автоматической пересборки контейнеров используйте скрипт `rebuild.sh`:

```bash
# Пересобрать только backend
./rebuild.sh backend

# Пересобрать только frontend
./rebuild.sh frontend

# Пересобрать все сервисы
./rebuild.sh all
# или просто
./rebuild.sh
```

---

## 🎯 Когда использовать

### Backend (`./rebuild.sh backend`)
Используйте когда изменили:
- ✅ `backend/server.py`
- ✅ `backend/auth.py`
- ✅ `backend/models.py`
- ✅ `backend/requirements.txt`
- ✅ Любые другие Python файлы в `backend/`

### Frontend (`./rebuild.sh frontend`)
Используйте когда изменили:
- ✅ Любые файлы в `frontend/src/`
- ✅ `frontend/src/components/*.jsx`
- ✅ `frontend/src/components/ui/*.jsx`
- ✅ `frontend/package.json`
- ✅ CSS/стили

### Все сервисы (`./rebuild.sh all`)
Используйте когда:
- ✅ Изменили и backend, и frontend
- ✅ Изменили `docker-compose.yml`
- ✅ Изменили Dockerfile'ы
- ✅ Нужна полная перезагрузка системы

---

## 📝 Что делает скрипт

1. **Останавливает** нужные контейнеры
2. **Пересобирает** образы с новым кодом
3. **Запускает** обновленные контейнеры
4. **Показывает** статус всех сервисов

---

## 🚀 Примеры использования

### Пример 1: Изменили backend API
```bash
# Отредактировали server.py, добавили новый endpoint
vim backend/server.py

# Пересобираем backend
./rebuild.sh backend

# Готово! Backend обновлен
```

### Пример 2: Изменили компонент React
```bash
# Отредактировали AdminPanelV2.jsx
vim frontend/src/components/AdminPanelV2.jsx

# Пересобираем frontend
./rebuild.sh frontend

# Готово! Frontend обновлен
```

### Пример 3: Изменили оба сервиса
```bash
# Изменили и backend, и frontend
vim backend/server.py
vim frontend/src/components/AdminPanelV2.jsx

# Пересобираем все
./rebuild.sh all

# Готово! Все обновлено
```

---

## ⚡ Альтернативные команды

Если скрипт не работает, используйте команды напрямую:

### Backend
```bash
docker-compose down backend
docker-compose build backend
docker-compose up -d backend
```

### Frontend
```bash
docker-compose down frontend
docker-compose build frontend
docker-compose up -d frontend
```

### Все сервисы
```bash
docker-compose down
docker-compose build
docker-compose up -d
```

---

## 🔍 Проверка статуса

После пересборки проверьте статус:

```bash
docker-compose ps
```

Должно быть:
```
NAME                  STATUS
numerom-v2-backend    Up X seconds
numerom-v2-frontend   Up X seconds
numerom-v2-mongodb    Up X minutes
```

---

## 🐛 Решение проблем

### Проблема: Скрипт не запускается
```bash
# Убедитесь что скрипт исполняемый
chmod +x rebuild.sh
```

### Проблема: Контейнер не запускается
```bash
# Посмотрите логи
docker-compose logs backend
docker-compose logs frontend
```

### Проблема: Изменения не применились
```bash
# Полная очистка и пересборка
docker-compose down -v
docker-compose build --no-cache
docker-compose up -d
```

### Проблема: Порты заняты
```bash
# Остановите все контейнеры
docker-compose down

# Проверьте что порты свободны
lsof -i :3000  # Frontend
lsof -i :8000  # Backend
lsof -i :27017 # MongoDB
```

---

## 📊 Время пересборки

- **Backend**: ~5-10 секунд
- **Frontend**: ~10-15 секунд (из-за npm build)
- **Все сервисы**: ~15-25 секунд

---

## 💡 Советы

1. **Пересобирайте только то, что изменили** - это быстрее
2. **Используйте `./rebuild.sh backend`** чаще всего (backend меняется чаще)
3. **Проверяйте логи** если что-то не работает: `docker-compose logs -f backend`
4. **Держите MongoDB запущенным** - его не нужно пересобирать

---

## 🎓 Запомните

> **Правило:** После изменения кода на backend или frontend **ВСЕГДА** пересобирайте контейнеры!

Без пересборки изменения **НЕ ПРИМЕНЯТСЯ**, так как Docker использует закешированные образы.

---

## 📞 Быстрая справка

```bash
# Backend изменен?
./rebuild.sh backend

# Frontend изменен?
./rebuild.sh frontend

# Оба изменены?
./rebuild.sh all

# Проверить статус
docker-compose ps

# Посмотреть логи
docker-compose logs -f backend
docker-compose logs -f frontend
```

---

**Готово!** Теперь вы знаете как правильно пересобирать контейнеры! 🚀

